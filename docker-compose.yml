version: '3'

services:

  discovery-service:
      build: discovery-service
      privileged: true
      network_mode: host
      volumes:
       - /tmp/data/:/data
      restart: on-failure


  rtsp-client:
    build: rtsp-client
    restart: always
    depends_on:
      - media-server
    volumes:
       - /tmp/data/:/data
    networks:
      - vms

  media-server:
    build: nginx-vms
    ports:
      - "1935:1935"
      - "8080:8080"
    volumes:
      - /tmp/data:/data
    networks:
      - vms

networks:
  vms:

